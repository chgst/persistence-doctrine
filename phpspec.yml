extensions:
  LeanPHP\PhpSpec\CodeCoverage\CodeCoverageExtension:
    blacklist: [ 'spec', 'vendor' ]
    format:
      - clover
      - html

    output:
      clover: build/logs/clover.xml
      html: build/coverage
